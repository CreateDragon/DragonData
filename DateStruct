一. 线性表，队列与栈
 普通的数据结构：线性表，栈，队列里面的原理性较简单。主要需要了解其头文件里的API函数。值得一提的是，队列和栈在树，图中的BFS(深度),DFS（广度）的应用。
稀疏矩阵也在下面介绍一下
  矩阵元素个数极多，但有用的元素较少时（例如0作为无用元素，可以不存入矩阵中）。采用一种方法：将非零元素及其相应的行和列构成一个三元组（行标，列标，值），
然后按某种规律存储这些三元组（一般是以矩阵的从左到右，从上向下）。
    
二. 查找
1.
